services:
  - type: web
    name: ecotasks
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: CLE_SECRETE
        generateValue: true
      - key: PYTHON_VERSION
        value: "3.11.0"
