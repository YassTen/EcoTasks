# Variables d'environnement pour EcoTasks
# Copiez ce fichier en .env et remplissez les valeurs

# Cle secrete pour signer les cookies de session Flask
# Generez une cle aleatoire : python -c "import os; print(os.urandom(24).hex())"
CLE_SECRETE=votre-cle-secrete-ici
